opinion_socket
==============

Creating a new poll
-------------------

1. Execute `rake new_poll[Summer Vacation Proposal]`.
2. Edit the file created in polls/ to add the desired description and the HTML
   for the form.

enrollments.yaml
----------------

This file contains a list of enrollment numbers of all the students.

Example:

    ---
    - b11018


conf.yaml
---------

Below is a list of supported configuration variables and their function:

* secret: used for generating the unique tokens.
* email:
  * sender: email address from which emails should be sent.
  * smtp: (all the options below are optional)
    * address:
    * port:
    * user_name:
    * password:
    * authentication:
    * domain:
* base_url: base url of the web application.
    
